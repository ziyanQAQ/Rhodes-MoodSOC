# 10 · 图形界面（`ui/`）

> 本文属于 `documents/`（**文档**目录）。代码在 `ui/`，计算规则仍以 `04`/`05` 与需求 docx 为准。
> 本文是**新增门类**（图形界面），不占用原 `AGENTS.md` 的 §编号。

---

## 1. 是什么 / 怎么启动

把本工具从"命令行算一次"变成"**看整周期**"：导入 MAA 排班 → 看板逐位置显示干员与实时心情 →
拖时间滑块看一天里心情怎么走 → 指定干员看整周期曲线。

```bash
.venv/Scripts/python.exe -m ui          # 启动图形界面
```

技术底座是 **tkinter（Python 标准库）**，因此"纯标准库、无第三方依赖"这条约束不变
（本项目不引入 PyQt/matplotlib；曲线是 Canvas 手绘的）。

启动后会自动载入示例排班 `resources/arknights-infra-schedule-maa.json`（存在时），
点「导入排班…」即可换成自己的文件。

---

## 2. 五个功能怎么用

| 需求 | 操作 |
|---|---|
| **① 导入多班**（12h/6h/6h 三个文件） | 「导入排班…」**可多选文件**；一个文件里含多个 plan 也会被当成多个班次（按文件顺序 + plan 顺序） |
| **① 自己设置周期/班数/每班时长** | 「班次设置…」：改周期时长与各班长；**各班长之和必须等于周期**，界面上实时校验（不满足时"应用"按钮禁用） |
| **② 逐位置设干员与心情** | 看板：**左键**位置 → 选人 / 更换 / 清空该位置；**右键**位置 → 设置该干员心情。也可在右侧面板「设置心情…」给当前曲线干员设心情 |
| **③ 时间滑动 → 各位置心情实时变化** | 底部滑块（也可 ◀▶ 15 分钟微调、`←/→` 键、`Home/End` 跳首尾）；心情值、心情条、颜色随时刻实时刷新 |
| **④ 对点：输入干员名 → 整周期曲线** | 右侧「对点查询」选人（下拉 / ◀▶ 切换）→ 曲线 + 关键数值（起点/周期末/最低·最高及时刻/红脸段数与合计时长/各班最低） |
| **⑤ 简洁明了** | 界面只有"看板 / 曲线 / 滑块"三块；配色与字体集中在 `ui/theme.py` |

附带的小工具：**▶ 播放**（让一个周期自动走一遍，直观看到"什么时候红脸"）、
**周期数 1/2/3**（连着跑几个周期，用来判断这套排班能不能永动）、
**结算进驻事件（M15a）** 开关（默认关，见 §5）。

---

## 3. 界面分区

```
┌──────────────────────────────────────────────────────────────────────────┐
│ 工具栏：导入排班… 班次设置… 重置心情 │ 周期数 │ 结算进驻事件 │ ▶播放 回到起点│
├───────────────────────────────────────┬──────────────────────────────────┤
│ 看板（可滚动）                         │ 对点查询 · 整周期心情曲线         │
│  ┌ 控制中枢 Lv1 5/5 ─────────────┐     │  干员 [巫恋 ▾] ◀ ▶   [设置心情…] │
│  │ [玛恩纳 7.2 ▍][维什戴尔 7.2 ▍]… │     │  ┌ 曲线（班次底色 + 红脸红底 +   │
│  └───────────────────────────────┘     │  │   最低/最高标注 + 当前时刻线）│
│  ┌ 制造站#1 Lv3 3/3 ─────────────┐     │  └──────────────────────────────┘
│  │ [泡泡 22.8 ▍][黍 …][路人甲 …]   │     │  起点 24　周期末 8.4             │
│  └───────────────────────────────┘     │  最低 8.4 @ 24:00　最高 24 @ 0:00│
│  …（制造/贸易/发电/会客/办公/训练/加工/宿舍各一行）│  红脸 1 段，合计 3.2h          │
├───────────────────────────────────────┴──────────────────────────────────┤
│ 班次 [1. Shift 1 · 12h][2. …6h][3. …6h]                                  │
│ ◀15min ────────────●──────────────────────────── 15min▶        12:00      │
│ 状态：3 班 / 周期 24h　干员 57 名　轨迹节点 130　重算耗时 172 ms            │
└──────────────────────────────────────────────────────────────────────────┘
```

- **看板**：每个房间一张卡片（房间名 · 等级 · 进驻 `n/容量`），卡内一位干员一个格子，
  显示**名字 + 当前时刻的心情**（保留两位小数）+ 心情条；心情 ≤ 0 的格子变红并标「红脸·技能失效」。
  房间排布按 `ui/board.py` 的 `ROW_ORDER`（控制中枢 → 制造 → 贸易 → 发电 → 会客/办公/训练/加工 → 宿舍），
  **要调排布只改这一处**；房间数与容量完全由导入的排班决定，不写死。
- **曲线**：班次交替底色 + 班次分界虚线 + 红脸区间红底；曲线是**事件节点直连的折线**
  （不做等距采样——等距采样会把拐点抹平）；鼠标悬停给十字准星与精确读数（时刻 / 心情 / 所在设施）。
- **滑块**：拖动只做插值刷新（不重算），节流约 30fps 且末位必落地。

---

## 4. 计算核心 `ui/schedule.py`（**不依赖任何 GUI**）

排班是"多班轮换"：一个 24h 周期切成若干班（12h + 6h + 6h），每班**自己的布局**，
干员在班次切换时换房间；心情跨班连续（**不在班次边界重置**）。

### 4.1 为什么是「事件驱动精确积分」

在一段区间内（没有事件发生），所有干员的净速率都不变 ⇒ 心情是**时间的线性函数**，
可以一步解析地推到区间末尾。会让速率改变的事有四类，都能**精确求出时刻**：

| 事件 | 为什么它改变速率 |
|---|---|
| **班次边界** | 布局换了，房间与同设施他人都变了 |
| **心情触到 0（红脸）** | 红脸后该干员所有心情类技能失效（`04-特殊机制.md` 第 1 条） |
| **心情触到阈值** `{24, 20, 18, 12}` | 现有条件函数与变量产出者读的就是这些值（<18 / <20 / ==24 / 落差>12 / mood_below_12） |
| **同设施两人心情交叉** | 单体回复的受益者按"心情最低且未满"锁定、池分配按"未满成员"均分 ⇒ 排序变化会换人 |

外加一条**安全上限** `MAX_SEGMENT_HOURS = 0.25h` 兜底：将来若新增"读心情的条件"而忘了登记到
`EVENT_THRESHOLDS`，误差也被限制在一个小段内（而不是无声放大）。
另外，**只在"可能变了"时才重算速率**（进新班次 / 发生事件）；纯上限推进直接复用上一次的速率。

**好处**：曲线由事件节点直连即为**精确**折线，滑块处线性插值取值也精确，且不必按 0.05h 跑上万步。

实测（示例排班，47 名干员）：

| 周期数 | 重算耗时 | 轨迹节点 | 与 0.05h 细步长对照的最大偏差 |
|---|---|---|---|
| 1 | ~0.17s | 130 | 0 |
| 2 | ~0.45s | 280 | — |
| 3 | ~0.65s | 430 | — |

> 精确性自证：把 `max_segment` 缩到 0.05h（重算次数 ×5）后，同一排班 24h 的心情**完全一致**
> （偏差 0.000000000000）——说明"事件切分"没有遗漏依赖心情的机制。

### 4.2 公开 API（可被测试/脚本直接用）

```python
from ui.schedule import load_schedule, simulate_schedule, Schedule, Shift

sch = load_schedule(["a.json", "b.json", "c.json"])      # 多文件；或一个含多 plan 的文件
#  → Schedule.shifts（每班 label/hours/facilities）、cycle_hours（= 各班长之和，强校验）
traj = simulate_schedule(sch, cycles=1, initial_moods=None, entry_events=False)
traj.mood_at("巫恋", 12)          # 精确取值（分段线性插值）
traj.bounds("巫恋")               # (最低, 最低时刻, 最高, 最高时刻)
traj.red_face_spans("巫恋")       # 红脸区间
traj.min_mood_at_each_shift("巫恋")   # 每班最低点（"哪一班最危险"）
traj.marks                        # 班次/进驻事件标记（画图用）
sch.with_hours(["8", "8", "8"])   # 改时长（返回新 Schedule）
sch.replaced_shift(1, facilities) # 改某个班次的布局（返回新 Schedule）
```

---

## 5. 口径与已知简化（写清楚，免得被当 bug）

1. **周期起点心情**：取"第一班布局里写的值"（场景格式支持 `{"name":"x","mood":10}`），
   没写就是满心情 24。界面上手动设的心情也是**周期起点**的值（对话框里已注明）。
2. **跨班不重置**：班次切换时心情接着上一班末尾继续（这才是排班能不能永动的关键）。
3. **未排班的干员**：某班次里没有他 ⇒ 视为"不在基建内"：净速率 0，心情不变。
4. **周期可重复**：周期数 2/3 表示把同一排班连跑几周，心情接着上一周期末尾继续。
5. **进驻事件（M15a 患难之交）**：默认**不结算**；勾选后在**每班开始时**结算一次（就地改心情，
   并在曲线面板标出）。理由同 CLI：它是"进驻那一刻"的事件，不是每小时速率。
6. **单体回复的锁定**仍是引擎既有的**简化**（全局一名受益者，见 `04-特殊机制.md` 建模假设 5）——
   本工具只是在时间上把它推下去，没有改这条口径。
7. **`M15b` 加工类（棘刺「爆炸艺术」）与加工站配方心情消耗**不参与，理由见 `04-特殊机制.md` 第 31 条。
8. 数值：引擎全程 `Decimal`；`Decimal` 除法在 28 位有效数字处舍入，跨事件分割会留下
   `16.19999999999999999999999999` 这类尾巴——**不是算错**，界面上在**显示边界**统一舍入到两位小数
   （`ui/theme.py: fmt_mood`），这与本项目"只在输出边界做一次受控舍入"的约定一致。

---

## 6. 代码结构（GUI 与引擎严格分离）

| 文件 | 职责 | 依赖 GUI？ |
|---|---|---|
| `ui/schedule.py` | 多班模型 + 整周期轨迹（事件驱动精确积分） | ❌ **纯计算** |
| `ui/theme.py` | 配色 / 字体 / 间距令牌 + 显示格式化 | ❌ |
| `ui/board.py` | 基建看板（房间卡片 + 位置上的干员名与心情） | ✅ |
| `ui/chart.py` | 心情曲线（Canvas 手绘：坐标轴 / 网格 / 班次分界 / 悬停读数 / 极值标注） | ✅ |
| `ui/dialogs.py` | 选人 / 设心情 / 班次设置三个对话框 | ✅ |
| `ui/app.py` | 主窗口：工具栏 + 看板 + 曲线 + 滑块 + 状态栏 | ✅ |
| `ui/__main__.py` | `python -m ui` 入口 | ✅ |

**结构性约束**：`import ui.schedule` **不得拉起 tkinter**——这条有测试盯着
（`tests/test_ui_schedule_blackbox.py::Test引擎不依赖GUI`），保证引擎能在无显示器环境被测试与复用。

`ui/schedule.py` 的房间映射与 `scripts/maa_to_scenario.py` 共用同一个解析模块
`mood_soc/maa.py`（MAA JSON → facilities），**不要再在别处复制那张映射表**。

---

## 7. 想改哪里

| 想改的东西 | 改哪儿 |
|---|---|
| 看板的房间行序 / 每行几个 | `ui/board.py` 的 `ROW_ORDER`、`PER_ROW` |
| 配色、字号、间距、心情色带 | `ui/theme.py` |
| 时间滑块的节流频率 | `ui/app.py` 的 `0.03`（秒） |
| 曲线的画法 / 悬停内容 | `ui/chart.py` |
| 积分精度与事件集合 | `ui/schedule.py` 的 `EVENT_THRESHOLDS` / `MAX_SEGMENT_HOURS` |
| MAA 房间映射 | `mood_soc/maa.py` 的 `ROOM_MAP` |

⚠️ 新增"读心情的条件函数"时，**要把它的阈值登记进 `EVENT_THRESHOLDS`**
（否则该条件的变化不会被当成事件切分，误差靠 0.25h 上限兜底）。

---

## 8. 测试

```bash
# 引擎黑盒（排班解析 / 周期校验 / 轨迹钳位与分段线性 / 对点取值 / 多文件导入 / 编辑接口）
.venv/Scripts/python.exe -m unittest tests.test_ui_schedule_blackbox -v

# 界面端到端冒烟（真建窗口；对话框打桩；无图形环境自动跳过）
.venv/Scripts/python.exe -m unittest tests.test_ui_app_smoke -v
```

自查清单（改 `ui/` 之后）：

- [ ] 引擎改动只落在 `ui/schedule.py`，没有把计算写进 `app.py`？
- [ ] `import ui.schedule` 仍然不依赖 tkinter（对应测试跑过）？
- [ ] 新增心情阈值条件 → 登记进 `EVENT_THRESHOLDS`？
- [ ] 改了看板/曲线的显示口径 → 同步本文与 `README.md`？
- [ ] 全量测试通过（`.venv/Scripts/python.exe -m unittest discover -s tests`）？
